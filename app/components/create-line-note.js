import Ember from 'ember';

export default Ember.Component.extend({
  page: '',
  line: '',
  note: '',
  error: ''
  /*actions: {
    addNote() {
      this.attrs.addNote(page, line, note, error)
    }
  }*/
});
