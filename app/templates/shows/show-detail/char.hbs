{{#if isEditing}}
  {{input attr-size=30 value=char.name
                enter=(action (mut char.name))}}
{{else}}
  {{char.name}}
{{/if}}

{{notes-list char=char
             notes=char.notes
             isShowing=isShowingCreateModal
             toggle=(action "toggleModal")
             addNote=(action "addNote" char)}}
