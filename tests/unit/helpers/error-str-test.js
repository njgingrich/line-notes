import { errorStr } from 'line-notes/helpers/error-str';
import { module, test } from 'qunit';

module('Unit | Helper | error str');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = errorStr([42]);
  assert.ok(result);
});
