import { imgSrc } from 'line-notes/helpers/img-src';
import { module, test } from 'qunit';

module('Unit | Helper | img src');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imgSrc([42]);
  assert.ok(result);
});
